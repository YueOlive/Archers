1. Always use latest Swift, SwiftUI, Combine, ARKit API
2. Use async / await API for asynchronous operations
3. Use Observation framework instead of @StateObject
4. Don't use any API that is marked Obsolete by Apple
